[platformOS:config] app/config.yml controls application behavior flags — changes require deploy.
Use recommended secure defaults (escape_output, safe_translate, slug_exact_match, etc.).
See the reference table below for all available flags and their effects.