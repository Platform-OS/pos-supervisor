{
  "description": "Module partials that use {% comment %} instead of {% doc %} for parameter docs, causing the LSP to infer required params from usage and emit false-positive MetadataParamsCheck on calling files. Diagnostic-pipeline suppresses those errors; this list marks which module paths are KNOWN offenders so we can distinguish them from new ones that still need upstream fixes.",
  "known": [
    "modules/user/helpers/can_do_or_unauthorized",
    "modules/user/helpers/can_do_or_redirect",
    "modules/user/helpers/can_do",
    "modules/common-styling/forms/error_list",
    "modules/common-styling/forms/error_input_handler",
    "modules/core/lib/validations/presence",
    "modules/core/commands/execute"
  ]
}
