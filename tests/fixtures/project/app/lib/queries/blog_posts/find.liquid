{% doc %}
  @param id {string} Record ID
{% enddoc %}

{% liquid
  graphql g = 'blog_posts/find', id: id
  assign results = g.records.results
  if results.size > 0
    return results[0]
  endif

  return null
%}
