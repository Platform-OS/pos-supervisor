query search($query: String, $page: Int = 1) {
  records(
    per_page: 20
    page: $page
    filter: {
      table: { value: "blog_post" }
      properties: [
        { name: "title", contains: $query }
      ]
    }
  ) {
    total_entries
    results {
      id
      properties
    }
  }
}
