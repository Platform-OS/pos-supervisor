{% doc %}
  @param object {object} - object with id
{% enddoc %}

{% liquid
  function object = 'commands/notifications/delete/check', object: object

  if object.valid
    function object = 'modules/core/commands/execute', mutation_name: 'notifications/delete', selection: 'record', object: object
  endif

  return object
%}
