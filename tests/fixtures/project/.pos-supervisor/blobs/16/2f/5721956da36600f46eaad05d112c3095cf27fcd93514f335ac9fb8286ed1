{% doc %}
  @param object {object} - form params
{% enddoc %}

{% liquid
  function object = 'commands/notifications/create/build', object: object
  function object = 'commands/notifications/create/check', object: object

  if object.valid
    function object = 'modules/core/commands/execute', mutation_name: 'notifications/create', selection: 'record', object: object
  endif

  return object
%}
