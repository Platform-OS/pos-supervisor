---
slug: events
method: delete
layout: application
---
{% liquid
  function object = 'queries/events/find', id: context.params.id
  function object = 'commands/events/delete', object: object

  if object.valid
    redirect_to '/events'
  else
    redirect_to '/events'
  endif
%}
