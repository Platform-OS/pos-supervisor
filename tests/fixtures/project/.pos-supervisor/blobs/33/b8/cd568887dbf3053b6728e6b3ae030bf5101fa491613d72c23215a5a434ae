'undefined_var' is undefined in this context.

STEP 1 — Identify what 'undefined_var' should be.
 Context data? → Use context.undefined_var — common properties:
 context.params (URL/form parameters)
 context.session (session data)
 context.current_user (authenticated user, nil if guest)
 context.page, context.location, context.environment
 GraphQL result? → {% graphql undefined_var = 'queries/...' %} then access undefined_var.records.results
 Function result? → {% function undefined_var = 'lib/...' %} then use return value
 Loop variable? → {% for undefined_var in collection %} — loop variables are declared by the
 for-tag itself. If linter fires here, 'undefined_var' is likely used
 outside the loop body (after {% endfor %}).
 Passed by caller? → You are likely in a partial — see UndefinedObject-partial hint.

STEP 2 — Apply the fix.

 → NO suggestion: check hover_docs field, or call domain_guide for the relevant domain.


Do not use bare 'undefined_var' — it must come from an assignment, context, GraphQL result, function return, loop variable, or caller parameter.
Fixing existing code: if 'undefined_var' is from another platform, replace it with the correct platformOS data source.
Building new code: identify where the data comes from and wire it in before using it.