Variable `undef_sev_test` is not defined. In partials, all variables must be passed explicitly. Add a `{% doc %}` block: `{% doc %} @param {object} undef_sev_test {% enddoc %}` and pass it from the caller.