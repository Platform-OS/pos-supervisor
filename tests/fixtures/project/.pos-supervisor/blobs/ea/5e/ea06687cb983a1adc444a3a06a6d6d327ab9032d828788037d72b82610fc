---
slug: events
method: post
layout: application
---
{% liquid
  function object = 'commands/events/create', object: context.params.event

  if object.valid
    redirect_to '/events'
  else
    render 'events/new', object: object
  endif
%}
