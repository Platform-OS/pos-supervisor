{% doc %}
  @param object {object} - form params with id
{% enddoc %}

{% liquid
  function object = 'commands/events/update/build', object: object
  function object = 'commands/events/update/check', object: object

  if object.valid
    function object = 'modules/core/commands/execute', mutation_name: 'events/update', selection: 'record', object: object
  endif

  return object
%}
