{% doc %}
  @param object {object} - form params to reshape
{% enddoc %}

{% liquid
  assign object['id'] = object.id
  assign object['message'] = object.message
  assign object['read'] = object.read

  return object
%}
